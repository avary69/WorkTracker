<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WorkTracker">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Form label style -->
    <Style x:Key="RegularFormLabel" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="0 12 10 12" />
    </Style>
    
    <!-- Textbox style -->
    <Style x:Key="RegularFormTextBox" TargetType="TextBox">
        <Setter Property="Margin" Value="15 7 0 7" />
        <Setter Property="Padding" Value="5 0 0 0" />
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style x:Key="RegularFormComboBox" TargetType="ComboBox">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="15 7 0 7" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="RegularFormButton" TargetType="Button">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Margin" Value="15 21 80 00" />

    </Style>

</ResourceDictionary>